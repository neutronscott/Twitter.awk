20110726
done	* CREATE CONF FILE for TOKENS and SAVE NEW ONES there
	  "use openssl"
done	* need function to read_header_status(), return HTTP status code.
	  stop at \n\n
nah	* put oauth_header() in http_make_cmd ?
nah	* create table for conv_hex2bin(conv_str2hex(s)) ? can't with WCHAR eh?
	  maybe table ASCII and call conv_hex2bin(conv_str2hex()) is missing
ok	* put hex comments in sha1sum *shrug*
nah	* fix that padding loop in sha1sum heh.
done?	* replace curl with openssl client? :o
